import { LeagueTierToStringyPipe } from './league-tier-to-stringy-pipe';

describe('LeagueTierToStringyPipe', () => {
  it('create an instance', () => {
    const pipe = new LeagueTierToStringyPipe();
    expect(pipe).toBeTruthy();
  });
});
